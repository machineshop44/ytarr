{
  "name": "ytarr",
  "install": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends python3.12-venv ffmpeg && python3 -m venv .venv && ./.venv/bin/python -m pip install --upgrade pip && ./.venv/bin/pip install -r backend/requirements.txt && npm --prefix frontend ci",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ../.venv/bin/python run.py"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0"
    }
  ],
  "ports": [
    { "name": "backend api", "port": 8199 },
    { "name": "frontend dev", "port": 5173 }
  ]
}
